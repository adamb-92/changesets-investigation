name: Version Package Two

on:
  pull_request:
    types: [labeled]
    branches:
      - "main"
      - "release*"
      - "hotfix*"

jobs:
  version-package-two:
    if: ${{ github.event.label.name == 'version-package-two' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm version-package-two"
